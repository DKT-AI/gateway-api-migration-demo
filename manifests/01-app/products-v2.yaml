apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-v2
  namespace: gatewaydemo
  labels:
    app: products
    version: v2
    app.kubernetes.io/name: products-v2
    app.kubernetes.io/part-of: vedmich-gatewaydemo
    app.kubernetes.io/managed-by: kubectl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products
      version: v2
  template:
    metadata:
      labels:
        app: products
        version: v2
        app.kubernetes.io/name: products-v2
        app.kubernetes.io/part-of: vedmich-gatewaydemo
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: products-v2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          image: ACCOUNT_ID.dkr.ecr.eu-central-1.amazonaws.com/vedmich-gatewaydemo/products-v2:latest
          ports:
            - containerPort: 8000
              protocol: TCP
          env:
            - name: SERVICE_NAME
              value: "products-v2"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: products-v2
  namespace: gatewaydemo
  labels:
    app: products
    version: v2
    app.kubernetes.io/name: products-v2
    app.kubernetes.io/part-of: vedmich-gatewaydemo
spec:
  selector:
    app: products
    version: v2
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
